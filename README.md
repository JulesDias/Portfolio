# Portfolio de Ludivine Rameaux

Ce repo contient le site portfolio (Next.js) dans le dossier `portfolio/`.

## 🚀 Démarrer

```bash
cd portfolio
npm install
npm run dev
```

Puis ouvrir http://localhost:3000

## 📚 Documentation

Voir `portfolio/README.md` pour le détail (routes/pages, assets, stack).
